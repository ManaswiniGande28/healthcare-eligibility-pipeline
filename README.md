# healthcare-eligibility-pipeline
Configuration-driven healthcare eligibility data pipeline that ingests partner files with different formats, standardizes member data, and produces a unified output dataset. Designed to easily onboard new partners through configuration changes without modifying core processing logic.
